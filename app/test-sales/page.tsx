import SalesComposition from "@/components/sections/SalesComposition";

// PÁGINA TEMPORÁRIA — só pra conferir o posicionamento da composição Sales Page.
export default function TestSales() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-forest">
      <SalesComposition />
    </main>
  );
}
